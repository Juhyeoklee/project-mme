# ── macOS
.DS_Store
._*

# ── 개인 데이터 방어선
# 스크린샷·스파이크 산출물은 이 레포 밖(상위 workspace)에 있다.
# 아래는 실수로 안쪽에 들어왔을 때를 대비한 이중 안전장치.
screenshots*/
spike/session-report.html

# ── Xcode / SPM
.build/
.swiftpm/
DerivedData/
xcuserdata/

# ── 시크릿
.env
.env.*
!.env.example
*.pem
*.p8
*.p12
*.mobileprovision
*.cer
