// MomentKernel — 사진별 (파일명, 파일 바이트) → 날짜·순간·장면 구조 (ADR 0001 결정 2 · ADR 0002).
// 이 모듈은 import 0이다. 플랫폼 프레임워크 금지는 scripts/verify-boundaries.swift가 강제한다.

/// 자리 표시 — 커널의 실제 타입과 시그니처는 커널 구현 세션에서 확정한다.
public enum Placeholder {}
