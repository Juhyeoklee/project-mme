# 모아노기 `moanogi`

게임 스크린샷을 자동으로 분류해 되돌아보고, 남기고 싶은 순간을 일기장처럼 꾸며
기록으로 남기는 앱. 본체는 SwiftUI 네이티브다(iOS·iPadOS부터). `project-mme`는 코드명이다.

## 이 저장소가 증명하려는 것

이직용 포트폴리오다 — 목표 포지션은 iOS 네이티브. 증명하려는 것은 둘이다(`docs/WHY.md` §1).

- **아키텍처 경계를 기계가 강제하게 만드는 설계.** 경계가 취향이 아니라 문제
  구조에서 나왔음을 보인다
- **에이전트가 코드를 생산하는 시대의 개발 워크플로우.** 생산 속도가 올라갈수록
  아키텍처 경계가 유일한 품질 게이트가 된다는 문제의식과, 그걸 실제로 강제하는 장치

## 구조

```
┌──────────────────────────────────┐
│ App — 화면 · 내비 · 오케스트레이션 │
└───────┬──────────────┬───────────┘
        ▼              ▼
 ┌─────────────┐  ┌──────────────┐
 │ PhotoSource │  │ MomentKernel │
 │ Photos 유일 │  │   import 0   │
 └─────────────┘  └──────────────┘
```

- **`MomentKernel`** — 자동 분류. 순수 함수, **import 0**. 픽셀을 디코딩하지 않는다(`0004`)
- **`PhotoSource`** — 사진 라이브러리를 아는 유일한 모듈. **공개 API에 쓰기 연산이 없다**
  — 원본 불변이 테스트가 아니라 구조로 보장된다(`0001`)
- 구조의 진실은 `apple/Packages/*/Package.swift`다. 문서의 그림은 결정 시점의 스냅샷이다

**경계는 세 층이 지킨다**(`0003`) — 컴파일러(패키지 간 화살표) · 직접 만든
검증기(컴파일러가 못 막는 3항목, 에이전트의 수정 직후 훅 + CI) · 사람(의미 층).
CI와 같은 명령을 로컬에서 돌릴 수 있다 — `CLAUDE.md`의 「게이트」.

## 결정 기록

결정은 ADR로 남기고 고쳐 쓰지 않는다 — `docs/adr/`.

| | |
|---|---|
| `0001`~`0004` | 채택 — 모듈 경계 · 커널 입력 계약 · 에이전트 워크플로우 · 분류 신호 |
| `0005`~`0010` | 탈락 — 만들지 않은 것과 그 이유 (`WHY.md` §5) |

의도와 방향 전체는 `docs/WHY.md`에 있다.

## 기획·설계 문서는 왜 없는가

판단이지 누락이 아니다. 기획서·화면설계서·실험 기록은 비공개 워크스페이스에 있다.
구현과 함께 낡는 명세를 공개 저장소에 남기면 코드보다 문서가 먼저 눈에 띈다 —
저장소가 증명하는 것은 코드·테스트·CI·커밋 히스토리다.

## 에이전트 워크플로우

이 저장소 코드의 대부분은 에이전트가 생산한다. 경계 검증기가 에이전트의 파일 수정
직후 훅으로 돌아 위반을 생산 시점에 되돌려주고(`0003`), 사람은 의미 층 — 원칙의
해석 · 문서의 개정 · 커밋과 push — 을 잡는다. 에이전트를 위한 안내는 `CLAUDE.md`다.
