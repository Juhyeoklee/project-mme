# 코드 작업 안내

이 문서는 **상태를 말하지 않는다.** 진행 상태·다음 단계는 여기 없다 — 규칙과 포인터만 있다.

## 읽는 순서

1. `docs/WHY.md` — 왜 이 프로젝트를 이렇게 하는가
2. `docs/adr/` — 결정 기록. `0001` 모듈 경계 · `0002` 커널 입력 계약 · `0003` 에이전트 워크플로우 · `0004` 분류 신호 · **`0005`~`0010` 탈락 기록** — 막다른 길의 표지다. 같은 길을 다시 검토하기 전에 읽는다
3. **구조의 진실은 `apple/Packages/*/Package.swift`다.** 문서의 모듈 목록은 결정 시점의 스냅샷이다

## 경계 — 한 줄씩 (원문: ADR `0001`)

- `MomentKernel`은 **import 0** — 플랫폼·시간대·로케일을 모른다
- 사진 라이브러리를 아는 모듈은 `PhotoSource` 하나 — **공개 API에 쓰기 연산 없음** (원칙 `P5`)
- 커널 입력값의 **생산 과정도 계약이다** (결정 6) — 신호를 더할 때 이 문장부터 읽어라
- 컴파일러가 못 막는 3항목은 검증기가 막는다 — 게이트는 세 층, 늘리지도 줄이지도 않는다 (ADR `0003`)

## 게이트 — CI와 같은 명령을 직접 돌린다

```
cd apple && swift scripts/verify-boundaries.swift
swift test --package-path apple/Packages/MomentKernel
swift test --package-path apple/Packages/PhotoSource
xcodebuild build -project apple/Moanogi.xcodeproj -scheme Moanogi \
  -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO
```

빌드·테스트 루프에 도구를 더하지 않는다 (ADR `0003` 결정 3). 최종 차단은 컴파일러와 CI다.

이 블록은 `.github/workflows/ci.yml`의 사본이다 — 구조가 바뀌어 CI가 바뀌면 **이 블록을 같이 고친다.** 진실은 `ci.yml`이다.

## 주석 규칙 — `M1` 코드 세 종에서 추출 (2026-08-04)

**"무엇을 하는지"는 코드가 말하고, 주석은 왜 · 계약 · 출처를 말한다.**
그리고 **주석은 결론 + 포인터다** — 서사는 원문(ADR · 커밋 · 문서 §)에 두고 가리킨다. 이력은 git이 말한다.
이름이 이미 말하는 것은 다시 말하지 않는다. 기존 코드의 주석 밀도가 이 규칙과 어긋나면 규칙이 우선한다.

1. **파일 머리 `//`** — 역할 한 줄 + 이 파일이 구현하는 결정과 출처(ADR·문서 §). 방식이 특이하면 왜 이 방식인지까지
2. **공개 API `///`는 계약을 말한다** — 시그니처가 못 담는 불변식·함의. 동작 요약의 반복이 아니다
3. **근거 있는 값에는 출처를 단다** — 실측치·실험 §·확정 문서 §·날짜. 값의 유래와 재검토 조건까지
4. **백틱 ID로 문서와 십자 참조한다** — 요구(`MOM-01`) · 화면(`04-C3`) · 원칙(`P5`) · 리스크(`R8`) · ADR 번호
5. **안 하는 것에는 이유를 적는다** — *"여기서 값을 발명하지 않는다"* · *"술어로 제목을 거르지 않는다"*
6. **갈림에는 안전한 실패 방향을 적는다** — *"덜 쪼개는 쪽이 안전한 실패 방향이라 위끝을 잡았다"*
7. **미래 변경 지점에 전언을 남긴다** — 다음 마일스톤·다음 조정이 올 때 그 자리에서 읽을 문장. *"첫 조정 레버가 여기다"*
8. **관측 주장에는 날짜를 단다** — *"검증 기기에 실제로 있었다(2026-08-03)"*
9. **테스트 이름은 한국어 서술문이다** — 테스트가 곧 명세 문장. `판정_단위가_인접쌍이_아니라_군집이다`
10. **문체** — 주석은 한국어 단정형 문어체, 식별자는 영문, 강조는 `**볼드**`, ID·심볼은 백틱

## 기존 코드를 만나는 법

**기존 코드에 맞추느라 새 코드가 기이해지면, 그 기이함은 기존 코드의 결함 신호다.
우회를 완성하지 말고 멈춰서 보고하라.** 기존 코드는 "확정된 것"의 아우라를 갖지만,
판정이 실제로 덮은 범위는 생각보다 좁다. 보고 뒤는 결함의 지위로 갈린다 —

| 결함이 사는 층 | 처리 |
|---|---|
| **판정·계약이 걸린 자리** — 실기기 판정으로 확정된 분류 로직 · ADR 계약 · `MOM-*` 요구 | **멈추고 사용자 판정.** 판정을 통과한 로직의 결함 주장은 그 자체가 재판정이다. 고치기로 하면 **실물 전량 회귀 확인이 의무다** |
| **구현 세부** — 판정이 안 걸린 자리 (타입 · 파일 구성 · "구현 몫" 전부) | **바로 고치되, 고쳤음을 표시해 보고한다.** 묻어가지 않는다. 게이트 초록 유지가 조건 |

## 커밋 전 리뷰 — 시험 운행 (2026-08-05 도입)

코드 덩어리가 게이트를 통과하면, 커밋 전에 **생산 컨텍스트와 분리된 리뷰어**(신선한
컨텍스트의 서브에이전트 등)가 diff를 본다. **넷째 게이트 층이 아니다** — 커밋을 잡는
사람 층의 보조다(ADR `0003` 결정 1). 발견은 보고까지, 수정 여부는 세션과 사용자가 정한다.

리뷰어가 보는 것 —

1. **우회 냄새** — 새 코드가 기존 코드의 결함에 맞춰 기이해진 자리 (위 절. 우회는 만든 눈에 안 보이고 신선한 눈에 보인다)
2. **계약의 의미층** — 검증기가 못 보는 것: 커널 입력값의 생산 과정(ADR `0001` 결정 6) · 부분 입력 계약(ADR `0004`) · 원칙의 해석 — 원본 불변(`P5`) · 순간과 기록은 서로 모른다(`P2`) · 기록은 순간에서만 시작한다(`P3`)
3. **주석 규칙** — 위 10건 + 예산 규칙
4. **테스트가 명세인가** — 이름이 요구를 말하는가, 기대값이 구현의 복사가 아닌가

리뷰어가 안 하는 것 — **경계·설계 재심.** 그 판단은 사용자 몫이다.

## 도구

- **Xcode MCP `RenderPreview`** — ADR `0003`의 유보는 화면이 생긴 시점의 재판정에서 **채택**으로 뒤집혔다(2026-08-04, `aad0702`). 뒤집은 것은 `previewVariantOverrides`다 — Color Scheme·Dynamic Type을 호출 인자로 강제할 수 있다. 동작 조건: Xcode가 프로젝트를 열고 있을 것. 기준선: 콜드 ~91초 · 웜 2.5~10초
- **프리뷰 픽스처는 `static let`** — 갱신마다 새로 만들면 객체 신원이 바뀌어 무한 갱신 루프가 되고 Xcode가 통째로 물린다(2026-08-04 실측)
- **실기기 배포** — `apple/scripts/deploy-device.sh`. 기기 조회 → 빌드 → 설치 → 실행
- **편집 직후 진단은 믿는다** — `xcode-build-server` + 워크스페이스 루트의 `buildServer.json`이 `.xcodeproj` 타깃까지 인덱스를 연결한다(2026-08-04 채택 · 오탐 3종 해소 확인). 진단과 빌드가 어긋나면 진단을 무시하지 말고 빌드 서버 연결부터 의심한다

## 금지

- 개인 데이터·개발 기기 경로를 커밋하지 않는다 (`.gitignore`가 이중 안전장치다)
- 이 문서를 포함해, 상태는 어느 문서에도 적지 않는다
